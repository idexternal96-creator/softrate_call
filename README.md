# Softrate Backends

- `sales-backend`: existing DealVoice sales/admin backend.
- `crm-backend`: CRM admin microservice for converted clients, SLA/NDA, AMC, payments, tickets, and project placeholders.

Local scripts:

```sh
npm run dev:sales
npm run dev:crm
```
